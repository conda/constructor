Enhancements:
-------------

* <news item>

Bug fixes:
----------

* write to tempdir in installdir if normal tempdir is mounted as noexec
* use dd instead of `tail -c` for broader OS compatibility (`tail -c` doesn't work on SLES 12)

Deprecations:
-------------

* <news item>

Docs:
-----

* <news item>

Other:
------

* <news item>

