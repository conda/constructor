name: ExtraEnvs
version: X
installer_type: all
channels:
  - http://repo.anaconda.com/pkgs/main/
specs:
  - python=3.7
  - conda # conda is required for extra_envs
extra_envs:
  py38:
    specs:
      - python=3.8
  py39:
    specs:
      - python=3.9
